# Clarity Boost 使用手册

## 这是什么？

Clarity Boost 是一个适用于 Adobe Photoshop 2020 的清晰度增强插件。它会在 Photoshop 中增加一个面板，让你通过按钮快速增强图片清晰度。

它主要适合：

- 图片有一点发虚，想让边缘更清楚。
- 产品图需要更清晰的细节。
- 风景、建筑、纹理类图片需要增强质感。
- 修图后想保留一个可调节的增强图层。

## 第一步：下载插件

1. 打开本仓库页面。
2. 点击右上角绿色 `Code` 按钮。
3. 点击 `Download ZIP`。
4. 下载完成后，解压 ZIP 文件。

解压后你会看到这个文件夹：

```text
photoshop-clarity-boost
```

## 第二步：安装插件

1. 关闭 Photoshop。
2. 打开解压后的项目文件夹。
3. 进入：

```text
photoshop-clarity-boost
```

4. 在这个文件夹空白处按住 `Shift` 并点击鼠标右键。
5. 选择 `在此处打开 PowerShell 窗口`。
6. 运行下面这条命令：

```powershell
powershell -ExecutionPolicy Bypass -File .\Install-ClarityBoost.ps1
```

安装成功后，会显示插件安装目录。

## 第三步：如果插件没有显示

如果重启 Photoshop 后没有看到插件，请执行这一项。

1. 关闭 Photoshop。
2. 双击这个文件：

```text
photoshop-clarity-boost/enable-debug-mode-ps2020.reg
```

3. Windows 提示是否导入注册表时，选择确认。
4. 重新打开 Photoshop。

这是因为插件没有正式签名，Photoshop 2020 需要开启 CEP 调试模式后才能加载本地插件。

## 第四步：打开插件

在 Photoshop 2020 顶部菜单中打开：

```text
窗口 > 扩展功能 > Clarity Boost
```

如果你的 Photoshop 是英文界面，路径通常是：

```text
Window > Extensions > Clarity Boost
```

## 第五步：使用插件

1. 在 Photoshop 中打开一张图片。
2. 在图层面板中选中要处理的图层。
3. 打开 `Clarity Boost` 面板。
4. 选择一个增强模式。
5. 调整强度和边缘半径。
6. 点击 `应用清晰度增强`。

## 三种增强模式怎么选？

### 自然增强

推荐默认使用。适合人像、产品图、日常照片。

### 细节锐化

适合纹理、建筑、风景、布料、木纹、金属等细节比较多的图片。

### 强力清晰

适合轻微虚焦、边缘不明显、需要更强锐化效果的图片。强度过高可能出现光边，建议处理后降低增强图层透明度。

## 参数怎么调？

### 强度

控制增强效果的明显程度。

建议范围：

- 人像：30% 到 55%
- 产品图：45% 到 70%
- 风景图：50% 到 80%
- 轻微虚焦：60% 到 100%

### 边缘半径

控制插件识别边缘的范围。

建议范围：

- 人像：1.0 px 到 2.0 px
- 产品图：1.5 px 到 3.0 px
- 风景图：2.0 px 到 4.0 px
- 大尺寸图片：3.0 px 到 6.0 px

## 推荐设置

### 人像照片

```text
模式：自然增强
强度：40% 到 55%
边缘半径：1.0 px 到 2.0 px
减少彩色边缘：开启
保留可编辑增强图层：开启
```

### 产品图片

```text
模式：自然增强 或 细节锐化
强度：50% 到 70%
边缘半径：1.5 px 到 3.0 px
减少彩色边缘：开启
保留可编辑增强图层：开启
```

### 风景和建筑

```text
模式：细节锐化
强度：55% 到 80%
边缘半径：2.0 px 到 4.0 px
减少彩色边缘：开启
保留可编辑增强图层：开启
```

## 处理后怎么微调？

插件默认会保留一个增强图层。你可以在 Photoshop 图层面板中：

- 降低增强图层透明度，让效果更自然。
- 删除增强图层，恢复处理前效果。
- 给增强图层添加蒙版，只增强局部区域。

## 常见问题

### 运行 PowerShell 提示禁止执行脚本

使用这条命令安装：

```powershell
powershell -ExecutionPolicy Bypass -File .\Install-ClarityBoost.ps1
```

### 插件菜单里没有 Clarity Boost

请确认：

1. 已经运行安装脚本。
2. 已经导入 `enable-debug-mode-ps2020.reg`。
3. 已经重启 Photoshop。
4. 使用的是 Photoshop 2020。

### 效果太强怎么办？

降低插件里的 `强度`，或者处理后降低增强图层透明度。

### 图片出现彩色边缘怎么办？

勾选 `减少彩色边缘`，并适当降低强度。

### 可以用于 Photoshop 2021、2022 吗？

这个版本主要按 Photoshop 2020 制作。新版 Photoshop 推荐改成 UXP 插件结构。

## 卸载方法

关闭 Photoshop，然后删除这个文件夹：

```text
%APPDATA%\Adobe\CEP\extensions\com.codex.clarityboost
```

删除后重启 Photoshop，插件就不会再显示。

## 版权和商标说明

Clarity Boost 是第三方插件，不是 Adobe 官方插件。Adobe 和 Photoshop 是 Adobe 的商标，本项目只用于说明插件兼容 Adobe Photoshop 2020。
